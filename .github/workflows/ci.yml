# CI Pipeline — Lint, Test, Build
# Generated by claude-task-development-framework
#
# This workflow runs on pull requests and merge queue events.
# Replace [PLACEHOLDERS] with your project-specific values during
# /setup init or manually.

name: CI

on:
  pull_request:
    branches: [main, develop, staging]
  merge_group:
    types: [checks_requested]

permissions:
  contents: read
  pull-requests: write

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint-test-build:
    name: Lint, Test & Build
    runs-on: ubuntu-latest
    # Uncomment for matrix builds:
    # strategy:
    #   matrix:
    #     os: [ubuntu-latest, windows-latest, macos-latest]
    #     node-version: [18, 20, 22]
    # runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout code
        # Pin to commit SHA — never use @main or @v4 tags for security
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      # ── Runtime setup ──────────────────────────────────────────────
      # [CI_RUNTIME_SETUP] — replaced by /setup init
      # Example for Node.js:
      # - name: Setup Node.js
      #   uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4.4.0
      #   with:
      #     node-version: 22
      #     cache: npm

      # Example for Python:
      # - name: Setup Python
      #   uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5.6.0
      #   with:
      #     python-version: "3.12"
      #     cache: pip

      # ── Install dependencies ───────────────────────────────────────
      # [INSTALL_COMMAND] — replaced by /setup init
      # - name: Install dependencies
      #   run: npm ci

      # ── Lint ────────────────────────────────────────────────────────
      # [LINT_COMMAND] — replaced by /setup init
      # - name: Lint
      #   run: npm run lint

      # ── Test ────────────────────────────────────────────────────────
      # [TEST_COMMAND] — replaced by /setup init
      # - name: Run tests
      #   run: npm test

      # ── Build ───────────────────────────────────────────────────────
      # [BUILD_COMMAND] — replaced by /setup init
      # - name: Build
      #   run: npm run build
